name: Soli App Deployment to GitHub Pages

on:
  push:
    branches:
      - deploy-github  # Trigger on push to the deploy-github branch
  workflow_dispatch:  # Allow manual trigger from the Actions tab

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      # Stage 1: Checkout source code
      - name: Checkout repository
        run: |
          git clone https://github.com/${{ github.repository }} /tmp/repo
          pwd  # Log current directory for debugging
          ls -la /tmp/repo  # List contents for debugging

  build:
    runs-on: ubuntu-latest
    needs: checkout  # Depends on checkout job
    steps:
      # Stage 2: Set up environment and build
      - name: Set up Node.js
        run: |
          curl -fsSL https://deb.nodesource.com/setup_22.x | sudo bash -
          sudo apt-get install -y nodejs
          node --version  # Verify Node.js version (22.9.0)
          npm --version  # Verify npm is installed

      - name: Install dependencies
        run: |
          cd /tmp/repo
          npm install || exit 1

      - name: Build the app
        run: |
          cd /tmp/repo
          npm run build
          ls -la dist/ || echo "dist/ directory not found"  # Check if dist/ exists
          # Move dist/ to a shared location for the deploy job
          mv dist /tmp/dist-tmp
          ls -la /tmp/dist-tmp/ || echo "dist-tmp not found"  # Verify move worked

  deploy:
    runs-on: ubuntu-latest
    needs: build  # Depends on build job
    steps:
      # Stage 3: Deploy to gh-pages branch
      - name: Deploy to gh-pages branch
        run: |
          # Clone repo again for deployment (since we need a fresh git context)
          git clone https://github.com/${{ github.repository }} /tmp/deploy-repo
          cd /tmp/deploy-repo

          # Configure git
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          
          # Check if gh-pages branch exists, create it if not
          git fetch
          if git show-ref --quiet refs/heads/gh-pages; then
            git checkout gh-pages
          else
            git checkout --orphan gh-pages
          fi
          
          # Debug current directory
          pwd
          ls -la
          
          # Remove old files and copy new build output from tmp
          rm -rf ./*
          cp -r /tmp/dist-tmp/* . || { echo "Failed to copy /tmp/dist-tmp/*"; exit 1; }
          ls -la  # Verify files are copied
          
          # Ensure index.html is there
          cat index.html || echo "index.html not found"
          
          # Clean up temporary directory
          rm -rf /tmp/dist-tmp
          
          # Configure remote URL with GITHUB_TOKEN for authentication
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}
          
          # Commit and push to gh-pages
          git add .
          git commit -m "Deploy to GitHub Pages from deploy-github branch"
          git push origin gh-pages --force
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
